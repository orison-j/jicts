import styled from "styled-components";
import { Wrapper } from "./StyledWrappers";

export const CTAContainer = styled(Wrapper)`
    text-align: center;
    justify-content: center;
    padding: 40px;
`